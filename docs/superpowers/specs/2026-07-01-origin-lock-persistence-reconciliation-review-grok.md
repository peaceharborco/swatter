## Blockers
- Coexistence of csf-hook append (`-A`) + systemd standalone (teardown + `-I` prepends + preamble) under Option C is unsafe and unproven: standalone will teardown rules installed by csf hook and hoist its block (incl. lo/allow preambles) to absolute top of INPUT on independent fires, risking duplicates, changed ordering vs CSF accepts, or DROP/CF mis-order; install.sh does exclusive csfpre XOR systemd on CSF hosts; current service/docs claim unit not for CSF. file:install/install.sh:39-43 (if csf... _install_csfpre else systemd), lib/origin_lock.sh:319-321 (if [[ "$hook" != "csf" ]]; then swatter_origin_lock_teardown no), lib/origin_lock.sh:340-342 (if [[ "$hook" != "csf" ]]; then _ol_preamble...), install/swatter-origin-lock.service:4-5 (comment "On a CSF host ... this unit is not installed"), docs/superpowers/plans/2026-07-01-origin-lock-persistence-reconciliation.md:116 and 165 (Option C "must prove they don't duplicate/mis-order"), handoff.md:48-49 (static vs managed already problematic).
- Teardown-first in standalone apply (the systemd safety-net path) creates repeated fail-open + fail-closed windows on live chain: teardown removes CF-ACCEPT+DROP (non-CF exposed via CSF blanket accepts below), then reverse-emit starts with emit_drop `-I` (DROP prepended with no CF-ACCEPT yet, blackholing even CF traffic until emit_cf later prepends it), then preambles; sub-second but recurs on 5min timer and acknowledged as non-atomic. file:lib/origin_lock.sh:319-321 (teardown call), lib/origin_lock.sh:159-161 (if reverse: _ol_emit_drop; _ol_emit_log; _ol_emit_acme; _ol_emit_cf), lib/origin_lock.sh:340 (preamble after rules_family), docs/superpowers/plans/2026-07-01-origin-lock-persistence-reconciliation.md:317 (plan comment: "a sub-second window between teardown and the first DROP insert"), test/origin_lock_transition_test.sh:120-124 (final state only).
- Fail-open gate `_ol_ranges_healthy` is before teardown but teardown-first + silent iptables ops can still leave no lock or DROP-without-CF-ACCEPT: gate passes then teardown then build (no recheck), _ol_build_set and rule inserts swallow errors (2>/dev/null, no status check), so unhealthy transient after gate or partial emit can expose or blackhole; comment promises "install NOTHING" on bad ranges but code leaves prior rules. file:lib/origin_lock.sh:305-307 (if ! _ol_ranges_healthy; then return 1), lib/origin_lock.sh:319 (teardown only after), lib/origin_lock.sh:324-335 (_ol_build_set + _ol_rules_family unconditional), lib/origin_lock.sh:83-84 and 88 ( "installing NOTHING (fail-open)" ), lib/origin_lock.sh:154-157 (emits with no error handling).
- Gate sequencing in Task 2 (LOG with static still present, then flip drop, then retire) leaves two DROP owners + mixed enforcement during validation: static hand block (inline iptables, MODE=DROP) + managed hook coexist in csfpre (static code first), mutations (set LOG, csf -r, set drop, csf -r) before single-owner verify, potential ordering (static -I's vs managed -A after flush) or duplicate rules while static DROP protects during "LOG validation". file:docs/superpowers/plans/2026-07-01-origin-lock-persistence-reconciliation.md:125 (step3: "hand static block is still present (do NOT remove it yet)"), 127 (step5 retire after), 144 (LOG→verify→DROP→verify→retire→verify), install/install.sh:74-91 (csfpre wiring leaves non-marker content), handoff.md:48-55 and 100-101 (static + managed side-by-side, outage repro).
- Legacy retire `_ol_retire_legacy_static` (and supporting test) not implemented in tree; plan's heuristic ("cf_origin4 / ORIGIN-LOCK: outside managed markers") can miss (diff set name, limit 10/min vs 5, MODE=LOG, LOGPREFIX variant) or over-retire custom operator blocks; rollback relies on "re-add the static block from git history" but static block is untracked hand-written (never in repo). file:docs/superpowers/plans/2026-07-01-origin-lock-persistence-reconciliation.md:82-83 and 101 (Task1 sketch), install/install.sh:74-99 (only strips _managed_ markers via awk between BEGIN/END, no legacy detect), docs/superpowers/plans/2026-07-01-origin-lock-persistence-reconciliation.md:128 (rollback note), handoff.md:49-51 (static not from swatter).
- Standalone drop apply aborts (return 3, no-op) without `--yes` in non-interactive context (timer/oneshot): `if [[ "$(_ol_mode)" == "drop" && ... && ! assume_yes ]]` does preflight+error+return before calling apply; current service ExecStart lacks --yes (plan's proposed command has it but not in tree). file:lib/origin_lock.sh:470-486 (DROP guard + if (! assume_yes) { ... return 3 }), lib/origin_lock.sh:489 (apply call after), install/swatter-origin-lock.service:24 (ExecStart=/usr/local/bin/swatter origin-lock apply), docs/superpowers/plans/2026-07-01-origin-lock-persistence-reconciliation.md:119 and 126 (use --yes for re-arm).
- No timer file and install/service not updated for Option C: `install/swatter-origin-lock.timer` does not exist; service lacks `After=csf.service lfd.service`, `ExecStart` has no --yes, comments assert "not installed" on CSF; install.sh path is exclusive. file:install/swatter-origin-lock.service:1-32 (current content), install/install.sh:39-43 and 104-123, docs/superpowers/plans/2026-07-01-origin-lock-persistence-reconciliation.md:119-120 (create timer, adjust service/install).

## Majors
- Transition test fidelity limited: newline string + exact "$*" key match for -C/-I/-D approximates but does not model real iptables-legacy canonicalization (option ordering, multiport expansion, rule storage, first-match), trailing-space preservation under word-split vs argv, or concurrent csf interactions; green on model does not guarantee prod behavior for LOG delete or ordering. file:test/origin_lock_transition_test.sh:55-100 (full iptables() fake), 140-150 (asserts only final indices), lib/origin_lock.sh:211 and 218 (unquoted $r vs quoted LOG -D).
- Plan's prod steps for wiring managed hook rely on non-specified surgical mechanism (install.sh does marker rewrite but "never run install.sh remotely"; scp lib alone insufficient to add the guarded csfpre block). file:docs/superpowers/plans/2026-07-01-origin-lock-persistence-reconciliation.md:17 ( "surgical scp of individual libs ... never run install.sh remotely"), 125 (step3 "Install the managed csfpre hook"), install/install.sh:53-100 (the _install_origin_lock_csfpre logic using markers).
- Reboot/restart arming windows unaddressed: proposed timer `OnBootSec=2min` + csfpre does not guarantee origin-lock before CSF/lfd settle and network reachable; post-reboot or post-lfd window can expose origin until next fire (empirical char in plan step2 but code for timer absent). file:docs/superpowers/plans/2026-07-01-origin-lock-persistence-reconciliation.md:120 (OnBootSec=2min, OnUnitActiveSec=5min), install/swatter-origin-lock.service:18-19 (current After=network... only).
- Dual persistence will require coordinated changes to install.sh (to emit both on CSF), service, new timer, status/digest logic, disable, and docs; none of the Option C infra except Task0 diff is present. file:install/install.sh:184-186 (calls only one), lib/origin_lock.sh:441-447 and 604-607 (status only detects one), docs/superpowers/plans/2026-07-01-origin-lock-persistence-reconciliation.md:144-146 (Task4 update model).
- In teardown, allow/monitoring preamble -D are fire-and-forget single (no 20-try while like core rules), so duplicate -s accepts may survive repeated applies. file:lib/origin_lock.sh:231 ( "$ipt" -D ... || true  no loop), vs 207-213 (while for core).
- Report digest still greps csfpre for '^MODE=' (hand-static artifact); will be stale/empty after retire. file:lib/origin_lock.sh:580 (OL_MODE= grep '^MODE=' ...), 581 (fallback to _ol_mode).
- No post-mutation verification (e.g. rule count, order assert via -L or -C) in apply or cmd; relies on caller to check line numbers manually on prod. file:lib/origin_lock.sh:345-349 (just log_info after blind inserts).

## Minors
- Service file comment and description still describe "STANDALONE only, not on CSF" model contradicted by plan's Option C recommendation. file:install/swatter-origin-lock.service:4-8.
- Plan self-review flags the interaction risk but no regression test added exercising both hook=csf + standalone in same harness (only pure transition and call-stream tests). file:test/origin_lock_transition_test.sh:116 (standalone only), test/origin_lock_test.sh:58-100 (separate csf vs standalone).
- _ol_emit_log and LOG delete use 5/min while ground-truth static used 10/min (mismatch harmless but means "lingering LOG" root cause analysis version-specific). file:lib/origin_lock.sh:156,218.
- Version stamp (bin/swatter:2.3.0) vs prod 2.2.0 noted but Task3 separate; surgical deploy must also handle VERSION if stamped separately. file:bin/swatter:40.
- ACME and v6 paths have no equivalent teardown-first consideration documented for edge cases.
- No AGENTS.md or similar in tree for this subdir (but followed task scope).

VERDICT: Option C dual-carrier + teardown-first introduces unaddressed live exposure/blackhole windows, unproven coexistence, missing timer/install support, and unsafe gate/legacy sequencing that must be fixed (with tests and one-carrier preference or atomic apply) before prod steps; Task-0 ordering/LOG fix is real in code but its safety-net implications and LOG match on iptables-legacy unverified.
